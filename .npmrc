# npm workspace configuration
# Ensures consistent behavior across workspaces

# Use exact versions (optional, for stricter version control)
# save-exact=true

# Enable workspaces
workspaces-update=false

# Prevent hoisting of CRA webpack dependencies to fix monorepo issues
public-hoist-pattern[]=!*react-refresh*
public-hoist-pattern[]=!*babel-loader*
public-hoist-pattern[]=!*react-refresh-webpack-plugin*
