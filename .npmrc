# npm workspace configuration
# Ensures consistent behavior across workspaces

# Use exact versions (optional, for stricter version control)
# save-exact=true

# Enable workspaces
workspaces-update=false
